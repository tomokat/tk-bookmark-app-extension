// chrome.runtime.onMessage.addEventListener((message, sender, sendResponse) => {
//   if(message.action === "abc" && sender) {
//     sendResponse("Error");
//   }
// });

console.log('content script get called');